This is a GUI Java application that will allow the user to perform CRUD operations on a Ticket objects in database. GUI provides the necessary JavaFX components that will allow the user to enter and display the data. 

1. Insert information into the database from user input
2. Update the existing entry in the database from user input
3. Delete the existing entry in the database from user input
4. Display reports to user with all information from the table.  
5. Allow the user to select ID or Name, etc…  to view a particular entry. 

Prepared statements are used to implement all database operations.
